fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=1.10.0
python-dotenv>=1.0.0
PyYAML>=6.0
langgraph>=0.1.0
openai>=1.0.0

# Redis: persistent session memory + multi-process sharing
redis>=5.0.0

# Optional: Semantic memory (Tier 3 vector retrieval)
# Uncomment to enable embedding-based semantic search:
# sentence-transformers>=2.0.0
# chromadb>=0.4.0

# Optional: Improved vector math (Tier 2 TF-IDF)
# numpy>=1.24.0
